Listing 1:
  12 wird von "Serial.print()" ausgegeben, weil im Stantard des C das innerste Scope immer zuerst angesehen ist. An dieses Beispiel ist "int x=12" das innerste Scope.

Listing 2:
	12 wird von Serial.print() ausgegeben, weil nachdem die globale Variable "x" mit 12 aktualisiert worden ist, wird "Serial.print()" gerufen.

Listing 3:
  7 wird von "Serial.print()" ausgegeben, weil im Stantard des C das innerste Scope immer zuerst angesehen ist. An dieses Beispiel ist das Scope der "int x=12" Befehl schon geschlossen.

Listing 4:
	7 wird von "Serial.print()" ausgegeben, weil im Stantard des C das innerste Scope immer zuerst angesehen ist. An dieses Beispiel ist das Scope der "int x=newX" Befehl schon geschlossen wegen das geschlossene Funktion.

Listing 5:
	12 wird von Serial.print() ausgegeben, weil nachdem die globale Variable "x" mit 12 aktualisiert worden ist, wird "Serial.print()" gerufen.	
